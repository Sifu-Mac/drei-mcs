Hallo,

du wurdest zu DB × DREI eingeladen<%= @invite.client? ? " als Client." : " mit Administratorrechten." %>

Richte über folgenden Link dein Passwort ein und aktiviere deinen Zugang:
<%= invite_url(@invite.token) %>

Der Link ist bis <%= @invite.expires_at.utc.strftime("%d.%m.%Y um %H:%M UTC") %> gültig.

Falls du diese Einladung nicht erwartet hast, kannst du diese E-Mail ignorieren.
